{
  "name": "crickonnect-map",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://manikumar-ableaura.github.io/crickonnect-map/",
  "dependencies": {
    "@svgr/plugin-svgo": "^8.1.0",
    "cra-template": "1.2.0",
    "postcss": "^8.5.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-router-dom": "^6.28.1",
    "react-scripts": "^3.0.1",
    "resolve-url-loader": "^5.0.0",
    "svgo": "^3.3.2",
    "web-vitals": "^4.2.4"
  },
  "scripts": {
    
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
